Christian, el nuevo `repomix-output.xml` confirma algo importante: **todavía no hay código fuente real**, solo hay documentación en `00.md`, un `01.md` vacío y el driver JDBC en `jdbc/ojdbc8.jar`.

La implementación que se entregó antes sigue siendo válida como base, pero debe ajustarse a una regla clave del archivo:

> No se debe insertar nada en `ITEMS_ISSFA_CABECERA` desde esa consulta grande.
> Esa consulta sirve para comparar/reportar, pero la homologación real debe trabajar sobre:
>
> `SIS.ITEMS_ISSFA_DETALLE`
> `SIS.EQUIVALENCIAS_ITEMS_ISSFA`

---

# Corrección importante para el programador

## Problema detectado

En el archivo aparece un bloque tipo:

```sql
Insert into ITEMS_ISSFA_CABECERA (...)
Values (...);
```

Ese bloque es riesgoso porque mezcla campos de:

* `ITEMS_ISSFA_CABECERA`
* `ITEMS_ISSFA_DETALLE`
* `EQUIVALENCIAS_ITEMS_ISSFA`
* `ITEMS`

Eso parece una exportación de resultado de consulta, no un modelo correcto de inserción.

Por seguridad, el programador **no debe usar ese insert** para crear homologaciones.

---

# Instrucción ajustada

La aplicación PyQt6 debe hacer solamente esto:

## 1. Revisar si existe el código nuevo en `ITEMS_ISSFA_DETALLE`

```sql
SELECT *
FROM SIS.ITEMS_ISSFA_DETALLE
WHERE ID_ITISF = :id_itisf
  AND CODIGO = :codigo_actual;
```

Si no existe, debe insertar:

```sql
INSERT INTO SIS.ITEMS_ISSFA_DETALLE
    (ID_ITISF, CODIGO, DESCRIPCION, TIPO)
VALUES
    (:id_itisf, :codigo_actual, :descripcion_actual, :tipo);
```

---

## 2. Revisar si el código anterior existe en `ITEMS`

```sql
SELECT
    TIPO,
    SBS_SCC_CODIGO,
    SBS_CODIGO,
    CODIGO,
    DESCRIPCION,
    UNIDAD
FROM SIS.ITEMS
WHERE CODIGO = :codigo_anterior;
```

Si no existe en `SIS.ITEMS`, la fila debe quedar como:

```txt
NO_EXISTE_EN_ITEMS
```

y no se debe insertar equivalencia.

---

## 3. Revisar si ya existe la equivalencia

```sql
SELECT *
FROM SIS.EQUIVALENCIAS_ITEMS_ISSFA
WHERE TIPO = :tipo
  AND SBS_SCC_CODIGO = :sbs_scc_codigo
  AND SBS_CODIGO = :sbs_codigo
  AND CODIGO = :codigo_anterior
  AND ID_ITISF = :id_itisf
  AND CODIGO_ISSFA = :codigo_actual;
```

Si no existe, debe insertar:

```sql
INSERT INTO SIS.EQUIVALENCIAS_ITEMS_ISSFA
    (
        TIPO,
        SBS_SCC_CODIGO,
        SBS_CODIGO,
        CODIGO,
        ID_ITISF,
        CODIGO_ISSFA
    )
VALUES
    (
        :tipo,
        :sbs_scc_codigo,
        :sbs_codigo,
        :codigo_anterior,
        :id_itisf,
        :codigo_actual
    );
```

---

# Validación con Jaro-Winkler / Levenshtein

El algoritmo no debe decidir solo.

Debe servir como **alerta visual**.

Ejemplo:

| Resultado  | Acción                   |
| ---------- | ------------------------ |
| 97 a 100   | Alta confianza           |
| 88 a 96    | Revisar antes de aplicar |
| Menor a 88 | No recomendado           |

La comparación debe hacerse entre:

```txt
SIS.ITEMS.DESCRIPCION
```

contra:

```txt
Excel.DESCRIPCION_ACTUAL
```

---

# Flujo seguro obligatorio

La interfaz debe tener este flujo:

```txt
1. Login Oracle
2. Seleccionar Excel
3. Analizar
4. Mostrar vista previa
5. Marcar/desmarcar filas
6. Aplicar cambios
7. Commit si todo sale bien
8. Rollback si algo falla
9. Generar CSV de auditoría
```

---

# Backup SQL obligatorio antes de probar

```sql
CREATE TABLE SIS.BKP_ITEMS_ISSFA_DETALLE_20260424 AS
SELECT *
FROM SIS.ITEMS_ISSFA_DETALLE;
```

```sql
CREATE TABLE SIS.BKP_EQUIVALENCIAS_ITEMS_ISSFA_20260424 AS
SELECT *
FROM SIS.EQUIVALENCIAS_ITEMS_ISSFA;
```

Validación:

```sql
SELECT COUNT(*) FROM SIS.ITEMS_ISSFA_DETALLE;
SELECT COUNT(*) FROM SIS.BKP_ITEMS_ISSFA_DETALLE_20260424;

SELECT COUNT(*) FROM SIS.EQUIVALENCIAS_ITEMS_ISSFA;
SELECT COUNT(*) FROM SIS.BKP_EQUIVALENCIAS_ITEMS_ISSFA_20260424;
```

---

# Script de pánico

Solo si algo salió mal y se quiere volver al estado anterior:

```sql
DELETE FROM SIS.EQUIVALENCIAS_ITEMS_ISSFA;

INSERT INTO SIS.EQUIVALENCIAS_ITEMS_ISSFA
SELECT *
FROM SIS.BKP_EQUIVALENCIAS_ITEMS_ISSFA_20260424;

DELETE FROM SIS.ITEMS_ISSFA_DETALLE;

INSERT INTO SIS.ITEMS_ISSFA_DETALLE
SELECT *
FROM SIS.BKP_ITEMS_ISSFA_DETALLE_20260424;

COMMIT;
```

---

# Conclusión técnica para el programador

Debe implementar una app nueva porque el `repomix` no trae código existente, solo especificación.

La aplicación debe respetar estas tablas:

```txt
Lectura principal:
SIS.ITEMS

Creación / revisión:
SIS.ITEMS_ISSFA_DETALLE

Relación de homologación:
SIS.EQUIVALENCIAS_ITEMS_ISSFA
```

No debe usar `ITEMS_ISSFA_CABECERA` para insertar homologaciones. Esa tabla solo identifica la cabecera del tarifario mediante `ID`, fechas y observación.
